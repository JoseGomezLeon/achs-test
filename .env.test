TZ=UTC
PORT=3334
HOST=localhost
NODE_ENV=test

APP_KEY=zKXHe-Ahdb7aPK1ylAJlRgTefktEaACi
APP_URL=http://localhost:3334

LOG_LEVEL=error
SESSION_DRIVER=memory

# Base de datos — SQLite in-memory para tests unitarios
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# IoC: 'architecture' registra stubs (sin IO real), 'implementation' registra clientes reales
TEST_MODE=architecture

# Pact Broker local (docker-compose.test.yml)
PACT_BROKER_URL=http://localhost:9292

# Toxiproxy local (docker-compose.test.yml)
TOXIPROXY_URL=http://localhost:8474
